import { NameParserPipe } from './name-parser.pipe';

describe('NameParserPipe', () => {
  it('create an instance', () => {
    const pipe = new NameParserPipe();
    expect(pipe).toBeTruthy();
  });
});
